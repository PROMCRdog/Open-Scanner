# v0.2.0 test-evidence record

Date opened: 2026-08-02

Status: **language-selector code revision `97da15b` passes 112 JVM tests, 15 API 36 emulator instrumentation tests, release lint, package-boundary review, and the manual emulator locale protocol. A fresh independent audit is running. The previous signed/OPPO-installed candidate predates this change and is superseded; the final combined cache-independent gate, replacement signing, CI, and exact-candidate OPPO language/cadence/lifecycle/report-flow QA remain pending.** The unsigned release APK is not an installable handoff artifact.

## Source and device baseline

- The dedicated `codex/v0.2-fast-refresh` worktree began from `6dfbe2d`, **Add Simplified Chinese (zh-CN) localization**.
- After pull request #12 was merged, `codex/v0.2-language-selector` began from merged `main` revision `67e8a85`. Selector implementation is committed through `97da15b`.
- No device was attached when this record was opened. Later the same day, an authorized OPPO PMA120 on Android 16 / API 36 connected over USB. The permanent v0.1.0 installation was upgraded in place to the pre-selector candidate without uninstalling or substituting a debug signer. That installation is historical evidence only and does not validate the selector-inclusive build.
- Existing v0.1.0 test and signing results are historical baseline evidence only. They do not verify the v0.2.0 scheduler, fallback, translated UI, migration, or artifact.
- The v0.2.0 contract adds no one-second mode, permission, `INTERNET` access, enabled background work, or second scan loop. The selector adds only platform/AppCompat locale handling; the current source, dependency, and packaged-manifest audits confirm that boundary for code revision `97da15b`.

## Required static evidence

| Gate | Required evidence | Status |
|---|---|---|
| JVM behavior | 112 tests passed, zero skipped/failures/errors, including the prior scheduler/privacy/localization set plus clean default, supported-tag normalization, and unsupported-locale fail-closed policy | Passed on `97da15b` |
| Compose behavior | All 15 instrumentation tests passed on an Android 16 / API 36 emulator; the selector visibly defaults to System default and offers English and Simplified Chinese in addition to the prior fast-mode/version/accessibility coverage | Passed on selector code state |
| Runtime locale flow | Clean install has an empty app-locale override; English/Chinese apply immediately; Chinese persists after cold restart; empty System default follows later device-language changes from English to Chinese and back; reset clears the override | Passed on API 36 emulator with five seconds after every direct tap |
| Build | Strict dependency verification, JVM tests, debug APK, instrumentation APK, release lint, and unsigned release APK have passed individually; one final combined no-cache/rerun command remains before signing | Pending final combined rerun |
| Source hygiene | `git diff --check`, intended branch diff, no tracked signing/private material or machine-specific paths | Local audit passed; fresh independent audit and CI pending |
| Package boundary | No `INTERNET`, storage, notification, or background-location permission; no enabled/exported background service, worker, alarm, or scan component; the AndroidX locale metadata holder is disabled/non-exported and the unused EmojiCompat initializer is removed | Passed on merged release manifest |
| Artifact metadata | Package `org.openscanner.app`, version 2 / 0.2.0, min 26, target 36; replacement unsigned/signed hashes are not yet final | Static metadata passed; replacement candidate pending |

Executed repository gate:

```bash
./gradlew --dependency-verification=strict --no-build-cache --rerun-tasks \
  test \
  :app:assembleDebug \
  :app:assembleDebugAndroidTest \
  :app:lintRelease \
  :app:assembleRelease
git diff --check
```

Executed with the configured Android SDK supplied through `ANDROID_HOME` and `ANDROID_SDK_ROOT`:

- Temurin OpenJDK 17.0.8, Gradle 9.6.1, Android Gradle Plugin 9.3.1, Kotlin/Compose compiler plugin 2.3.21, Compose BOM 2026.06.00.
- Compile SDK 36.1, target SDK 36, minimum SDK 26.
- 112 JVM tests passed with zero skipped/failures/errors. A separate emulator run executed all 15 Compose instrumentation tests with zero skipped/failures/errors.
- Release lint completed with zero Error/Fatal findings and 22 warnings: six `GradleDependency`, two `NewerVersionAvailable`, eleven pre-existing localization/plural candidates, and one each for target SDK, a now-redundant v26 resource qualifier, and the launcher monochrome recommendation. No warning points to the selector or fast-refresh implementation.
- `git diff --check` passed. Compared with merged base `67e8a85`, the selector intentionally adds AppCompat 1.7.1 plus an explicit declaration of the already-resolved Startup 1.1.1 runtime; verification metadata pins the resolved artifacts. It adds no repository, mirror, dynamic version, permission, or Internet path.

Replacement artifact hashes and sizes will be recorded after the final combined gate. The following artifacts are from the superseded pre-selector code revision `00afee4` and must not be published:

| Artifact | Size | SHA-256 | Signature state |
|---|---:|---|---|
| `app-debug.apk` | 20,104,827 bytes | `736187a91dac42cb0af11808ad5c106c2f6da7cdd1bc458fccabde8605dd9905` | Debug certificate, APK Signature Scheme v2; installable only where its signer is accepted |
| `app-debug-androidTest.apk` | 1,110,816 bytes | `deff7de097d649f678f2dc91f625e6244a9f9a98058fb0940d3361eaebdcf89c` | Debug test artifact; all 14 tests passed on the API 36 emulator |
| `app-release-unsigned.apk` | 1,834,424 bytes | `d8f988852c208e9bf332370d197c4feac6830f32e2404d8d54db7c0bc0388d78` | Intentionally unsigned; not a distributable artifact |

The debug certificate SHA-256 is `f196f05e5e1b4905aeafa989c2db1a9f9130ca6423c623691fe6e0f4b0ac8c3c`; it is not the permanent v0.1.0 maintainer identity and must not replace the installed release. Current packaged permissions remain `ACCESS_COARSE_LOCATION`, `ACCESS_FINE_LOCATION`, `ACCESS_NETWORK_STATE`, `ACCESS_WIFI_STATE`, `CHANGE_WIFI_STATE`, and AndroidX's app-signature `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`. `MainActivity` is the only app-defined exported component. The cache `FileProvider`, AndroidX startup provider, and disabled AppCompat locale metadata holder are non-exported; AndroidX's profile-installer receiver remains protected by `android.permission.DUMP`. There is no enabled service or scheduled-work component.

An independent read-only review of the scheduler/freshness implementation found no remaining P0-P3 engine findings. A later release audit found inherited display-localization leaks, localized-search inconsistency, hidden-SSID ambiguity, and stale evidence copy; code revision `00afee4` resolved them. The re-audit at documentation revision `347b6bd` accepted that pre-selector code and signed candidate. A new independent audit is required for `97da15b` and its replacement artifact; the older acceptance must not be reused as selector evidence.

## Localization evidence

- English and Simplified Chinese each contain 379 named string/plural resources. The JVM parity test verifies identical keys, compatible resource kinds, and matching format placeholders while allowing locale-specific plural quantities.
- Simplified Chinese assertions cover hidden networks, privacy aliases, connected-network aliases, yes/no evidence, Enterprise/WPA3 Enterprise security, and Legacy/Unknown Wi-Fi generations.
- Search regression tests confirm that the localized visible network name is searchable and that an observed SSID literally named `Hidden network` is not mistaken for Android's hidden-network state.
- Android resource compilation and `xmllint` passed. AGP's generated locale configuration contains exactly `en` and `zh-CN`, and the packaged resource table includes `zh-rCN`.
- English and Simplified Chinese navigation, Settings, selector dialog, throttle explanations, and About/version copy were rendered and visually inspected on the API 36 emulator. App locale state was also checked through Android's locale manager. This does not replace the pending unlocked physical-device flow.

## Superseded signed candidate and upgrade evidence

The exact `00afee4` unsigned artifact with SHA-256 `d8f988852c208e9bf332370d197c4feac6830f32e2404d8d54db7c0bc0388d78` was signed outside Git with the existing permanent maintainer PKCS#12 identity. The key and password remain outside the repository. This candidate predates the required selector and is retained only as historical evidence.

| Candidate | Size | SHA-256 | Verification |
|---|---:|---|---|
| `Open-Scanner-v0.2.0.apk` | 1,853,287 bytes | `c4ee235deb446df879cfbba44a36ca855116fe2e3536a2e9f6a5d3c136d178a4` | Package `org.openscanner.app`, version 2 / 0.2.0, target 36; zip alignment passed; APK Signature Schemes v2 and v3 passed |

The signing certificate SHA-256 is `3d170c6970999581227c7f0f4423564310ea2afd9b8d38d72d046a4c314b489f`, matching the permanent v0.1.0 maintainer identity. `adb install -r` upgraded the authorized OPPO PMA120 without uninstalling it: `firstInstallTime` remained unchanged, the package-manager signature identity remained unchanged, and the installed base APK SHA-256 exactly matched the signed candidate. A forced cold start returned `LaunchState: COLD`, created the app process, and produced no matching fatal/ANR entry; the secure lock screen prevented visible UI and the required redacted/unredacted report interactions.

The replacement selector-inclusive candidate has not yet been signed or installed on OPPO. The historical hash above must not be attached to the v0.2.0 release.

## Required truthfulness assertions

- Five seconds becomes selectable only after Android explicitly reports Wi-Fi scan throttling off.
- An enabled or unavailable capability disables it; a saved five-second value is held during capability discovery, then persisted as 30 seconds and immediately rescheduled to 30 seconds if discovery ends in an ineligible state.
- At most one request is in flight. Late completion cannot overlap the next request, manual refresh is coalesced, and a 15-second timeout permits recovery.
- Leaving the foreground or pausing stops fast requests; returning or resuming restarts only the one application coordinator.
- Only a successful updated broadcast with advancing platform timestamps, or a successful newly empty result, advances snapshot sequence/capture time and downstream history/logging.
- Rejected, explicitly non-updated, unchanged-timestamp, cached, and timed-out cycles retain the previous observation identity while updating request flags and connection evidence.
- `likelyThrottled` reflects the latest request cycle and remains distinct from Android's configured throttle switch.

## Pending physical-device protocol

Use an authorized Android 10+ device and the exact final candidate. Wait five seconds after every direct UI tap.

1. Verify Settings starts at System default after upgrade unless Android already has an app-specific locale; exercise English, Simplified Chinese, cold-restart persistence, and reset-to-system-default.
2. With scan throttling ON, confirm the 5 s option is visible, disabled, and explained.
3. Turn scan throttling OFF, return to Open Scanner, choose 5 s, and collect a two-minute redacted scan-flags/source-age log.
4. Correlate request starts, completion/timeout cycles, platform source timestamps, sequence IDs, and visible source age. Confirm no overlap and no fake history point for cached evidence.
5. Turn throttling ON and verify the persisted/scheduled interval automatically returns to 30 seconds.
6. Exercise pause/resume, background/foreground, process restart, manual refresh during an in-flight request, and timeout recovery.
7. Install as an upgrade from the permanently signed v0.1.0 artifact and verify preference migration, package identity, signing lineage, cold launch, and no crash/ANR.

Record the device model, Android/API level, OEM build, throttle state, test duration, requested cadence, observed fresh-result cadence, battery/performance observations, screenshots/log hashes, and tester. The result must not claim that disabling Android throttling guarantees a fresh hardware scan every five seconds.
