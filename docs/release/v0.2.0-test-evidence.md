# v0.2.0 test-evidence record

Date opened: 2026-08-02

Status: **local static/build and localization gates, emulator instrumentation, permanent signing, byte-identical in-place installation, a process-level cold start, and final independent review passed for code revision `00afee4`; CI, the full unlocked physical cadence/lifecycle protocol, and redacted/unredacted report-flow QA remain pending.** The unsigned release APK is not an installable handoff artifact.

## Source and device baseline

- The dedicated `codex/v0.2-fast-refresh` worktree began from `6dfbe2d`, **Add Simplified Chinese (zh-CN) localization**.
- No device was attached when this record was opened. Later the same day, an authorized OPPO PMA120 on Android 16 / API 36 connected over USB. The permanent v0.1.0 installation was upgraded in place to the exact final v0.2.0 candidate without uninstalling or substituting a debug signer.
- Existing v0.1.0 test and signing results are historical baseline evidence only. They do not verify the v0.2.0 scheduler, fallback, translated UI, migration, or artifact.
- The v0.2.0 contract adds no one-second mode, permission, `INTERNET` access, background work, or second scan loop. The current source, dependency, and packaged-manifest audits confirm that boundary for code revision `00afee4`.

## Required static evidence

| Gate | Required evidence | Status |
|---|---|---|
| JVM behavior | 107 tests passed, zero skipped/failures/errors, including interval policy, delayed capability resolution, fallback, cadence, timeout, lifecycle cancellation, coalescing, overlap prevention, serialized result publication, freshness watermark/reuse behavior, localization parity, localized search, and explicit hidden-SSID semantics | Passed on `00afee4` |
| Compose behavior | All 14 instrumentation tests passed on an Android 16 / API 36 emulator; fast-mode OFF/ON/unavailable/unresolved semantics, selection, warning copy, and dynamic About version are covered | Passed on `00afee4` |
| Build | Strict dependency verification with all tasks rerun without the build cache: JVM tests, debug APK, instrumentation APK, release lint, and unsigned release APK | Passed on `00afee4` in 1m 44s |
| Source hygiene | `git diff --check`, intended branch diff, no tracked signing/private material or machine-specific paths | Local and independent audits passed; CI pending |
| Package boundary | Packaged release has no `INTERNET`, storage, notification, background-location, service, or app-defined background scanning component; no manifest/dependency additions relative to `6dfbe2d` | Passed |
| Artifact metadata | Package `org.openscanner.app`, version 2 / 0.2.0, min 26, target 36, and local unsigned/debug hashes recorded below | Static metadata and permanent signature passed |

Executed repository gate:

```bash
./gradlew --dependency-verification=strict \
  test \
  :app:assembleDebug \
  :app:assembleDebugAndroidTest \
  :app:lintRelease \
  :app:assembleRelease
git diff --check
```

Executed with the configured Android SDK supplied through `ANDROID_HOME` and `ANDROID_SDK_ROOT`:

- Temurin OpenJDK 17.0.8, Gradle 9.6.1, Android Gradle Plugin 9.3.1, Kotlin/Compose compiler plugin 2.3.21, Compose BOM 2026.06.00.
- Compile SDK 36.1, target SDK 36, minimum SDK 26.
- `BUILD SUCCESSFUL in 1m 44s`; 107 JVM tests passed with zero skipped/failures/errors. A separate emulator run executed all 14 Compose instrumentation tests with zero skipped/failures/errors.
- Release lint completed with zero Error/Fatal findings and 22 warnings: six `GradleDependency`, two `NewerVersionAvailable`, eleven pre-existing localization/plural candidates, and one each for target SDK, a now-redundant v26 resource qualifier, and the launcher monochrome recommendation. No warning points to the fast-refresh scheduler or setting implementation.
- `git diff --check` passed. Compared with base `6dfbe2d`, dependency catalogs, verification metadata, root/module dependency declarations, and the production manifest are unchanged; `app/build.gradle.kts` changes only the version code/name.

Local artifact hashes and sizes from code revision `00afee4`:

| Artifact | Size | SHA-256 | Signature state |
|---|---:|---|---|
| `app-debug.apk` | 20,104,827 bytes | `736187a91dac42cb0af11808ad5c106c2f6da7cdd1bc458fccabde8605dd9905` | Debug certificate, APK Signature Scheme v2; installable only where its signer is accepted |
| `app-debug-androidTest.apk` | 1,110,816 bytes | `deff7de097d649f678f2dc91f625e6244a9f9a98058fb0940d3361eaebdcf89c` | Debug test artifact; all 14 tests passed on the API 36 emulator |
| `app-release-unsigned.apk` | 1,834,424 bytes | `d8f988852c208e9bf332370d197c4feac6830f32e2404d8d54db7c0bc0388d78` | Intentionally unsigned; not a distributable artifact |

The debug certificate SHA-256 is `f196f05e5e1b4905aeafa989c2db1a9f9130ca6423c623691fe6e0f4b0ac8c3c`; it is not the permanent v0.1.0 maintainer identity and must not replace the installed release. The packaged release permissions are `ACCESS_COARSE_LOCATION`, `ACCESS_FINE_LOCATION`, `ACCESS_NETWORK_STATE`, `ACCESS_WIFI_STATE`, `CHANGE_WIFI_STATE`, and AndroidX's app-signature `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`. `MainActivity` is the only app-defined exported component; the cache `FileProvider` and AndroidX startup provider are non-exported, while AndroidX's existing profile-installer receiver is protected by `android.permission.DUMP`. There are no services or scheduled-work components.

An independent read-only review of the scheduler/freshness implementation found no remaining P0-P3 engine findings. A later release audit found inherited display-localization leaks, localized-search inconsistency, hidden-SSID ambiguity, and stale evidence copy. Code revision `00afee4` resolves them by carrying Android's explicit hidden state separately from SSID text; filtering on the localized visible name; and retaining semantic private names, nullable unavailable BSSIDs, Wi-Fi generation enums, and Boolean values until the localized UI boundary. The final independent re-audit at documentation revision `347b6bd` passed with no remaining P0-P3 findings and accepted the corrected signed candidate; CI and physical behavior remain separate gates.

## Localization evidence

- English and Simplified Chinese each contain 369 named string/plural resources. The JVM parity test verifies identical keys, compatible resource kinds, and matching format placeholders while allowing locale-specific plural quantities.
- Simplified Chinese assertions cover hidden networks, privacy aliases, connected-network aliases, yes/no evidence, Enterprise/WPA3 Enterprise security, and Legacy/Unknown Wi-Fi generations.
- Search regression tests confirm that the localized visible network name is searchable and that an observed SSID literally named `Hidden network` is not mistaken for Android's hidden-network state.
- Android resource compilation and `xmllint` passed. The packaged resource table includes `zh-rCN`.
- English and Simplified Chinese navigation, Settings, throttle explanations, and About/version copy were rendered and visually inspected on the API 36 emulator. This does not replace the pending unlocked physical-device flow.

## Signed candidate and upgrade evidence

The exact `00afee4` unsigned artifact with SHA-256 `d8f988852c208e9bf332370d197c4feac6830f32e2404d8d54db7c0bc0388d78` was signed outside Git with the existing permanent maintainer PKCS#12 identity. The key and password remain outside the repository.

| Candidate | Size | SHA-256 | Verification |
|---|---:|---|---|
| `Open-Scanner-v0.2.0.apk` | 1,853,287 bytes | `c4ee235deb446df879cfbba44a36ca855116fe2e3536a2e9f6a5d3c136d178a4` | Package `org.openscanner.app`, version 2 / 0.2.0, target 36; zip alignment passed; APK Signature Schemes v2 and v3 passed |

The signing certificate SHA-256 is `3d170c6970999581227c7f0f4423564310ea2afd9b8d38d72d046a4c314b489f`, matching the permanent v0.1.0 maintainer identity. `adb install -r` upgraded the authorized OPPO PMA120 without uninstalling it: `firstInstallTime` remained unchanged, the package-manager signature identity remained unchanged, and the installed base APK SHA-256 exactly matched the signed candidate. A forced cold start returned `LaunchState: COLD`, created the app process, and produced no matching fatal/ANR entry; the secure lock screen prevented visible UI and the required redacted/unredacted report interactions.

## Required truthfulness assertions

- Five seconds becomes selectable only after Android explicitly reports Wi-Fi scan throttling off.
- An enabled or unavailable capability disables it; a saved five-second value is held during capability discovery, then persisted as 30 seconds and immediately rescheduled to 30 seconds if discovery ends in an ineligible state.
- At most one request is in flight. Late completion cannot overlap the next request, manual refresh is coalesced, and a 15-second timeout permits recovery.
- Leaving the foreground or pausing stops fast requests; returning or resuming restarts only the one application coordinator.
- Only a successful updated broadcast with advancing platform timestamps, or a successful newly empty result, advances snapshot sequence/capture time and downstream history/logging.
- Rejected, explicitly non-updated, unchanged-timestamp, cached, and timed-out cycles retain the previous observation identity while updating request flags and connection evidence.
- `likelyThrottled` reflects the latest request cycle and remains distinct from Android's configured throttle switch.

## Pending physical-device protocol

Use an authorized Android 10+ device and the exact final candidate. Wait five seconds after every direct UI tap.

1. With scan throttling ON, confirm the 5 s option is visible, disabled, and explained.
2. Turn scan throttling OFF, return to Open Scanner, choose 5 s, and collect a two-minute redacted scan-flags/source-age log.
3. Correlate request starts, completion/timeout cycles, platform source timestamps, sequence IDs, and visible source age. Confirm no overlap and no fake history point for cached evidence.
4. Turn throttling ON and verify the persisted/scheduled interval automatically returns to 30 seconds.
5. Exercise pause/resume, background/foreground, process restart, manual refresh during an in-flight request, and timeout recovery.
6. Install as an upgrade from the permanently signed v0.1.0 artifact and verify preference migration, package identity, signing lineage, cold launch, and no crash/ANR.

Record the device model, Android/API level, OEM build, throttle state, test duration, requested cadence, observed fresh-result cadence, battery/performance observations, screenshots/log hashes, and tester. The result must not claim that disabling Android throttling guarantees a fresh hardware scan every five seconds.
