# Open Scanner v0.2.0 — Truthful 5-second refresh

Open Scanner v0.2.0 is an early public release of the passive, local-only Android Wi-Fi analyzer. Its main addition is a foreground **5 s request mode** that is available only when Android explicitly reports Wi-Fi scan throttling as off.

## What is new

- Exact refresh choices are 5, 10, 15, 30, and 60 seconds; 30 seconds remains the default.
- The app permits only one scan request in flight, coalesces manual refreshes, recovers from a stalled request after 15 seconds, and stops fast requests when paused or backgrounded.
- If five seconds was saved and Android later reports throttling on or unavailable, the app persists and schedules the 30-second fallback after capability discovery.
- Charts, history, sequence IDs, and logs advance only for newly timestamped scan evidence. Cached, rejected, unchanged, and timed-out cycles remain visibly stale instead of creating fake samples.
- Settings can immediately select **System default**, **English**, or **Simplified Chinese**. System default is the clean-install/reset choice and continues following later device-language changes.
- English and Simplified Chinese cover hidden/private network names, connection evidence, security profiles, Wi-Fi generations, fast-mode explanations, accessibility descriptions, and dynamic version copy.

## Privacy and safety boundary

- No `INTERNET` permission, telemetry, ads, accounts, background service, wake lock, port scanning, LAN probing, password handling, or one-second mode was added.
- Open Scanner does not join networks. Android owns connection and protected Developer options.
- Report redaction remains on by default. Unredacted sharing still requires an explicit warning and review of the exact payload before Android's share sheet opens.

## Verification scope

- 112 JVM tests and 15 API 36 Compose instrumentation tests passed with zero failures or skips.
- A final strict, cache-independent build reran 270 tasks covering JVM tests, debug/test APKs, release lint, and unsigned release assembly. Release lint reported zero errors.
- An independent read-only verifier accepted the selector-inclusive code with no remaining P0-P3 finding.
- API 36 emulator and OPPO testing confirmed System default, English, and Simplified Chinese selection, immediate switching, cold-restart persistence, and reset behavior.
- On the OPPO PMA120, upgrading the permanent-signer v0.1.0 installation preserved package/signer identity and seeded preferences. The installed APK matched the release candidate byte-for-byte and cold-launched without an observed crash or ANR.
- With the actual ColorOS scan-throttling switch off, a redacted 155,013 ms trace measured 31 steady result deltas from 4,877 to 5,138 ms (median 5,004 ms). Turning throttling back on automatically restored the 30-second interval and disabled five seconds.
- Pause/resume, background/foreground, process restart, manual refresh, localized report-warning cancellation, exact redacted CSV preview, and Android share-sheet handoff passed on the OPPO. Every direct device tap used the required five-second response wait.

This is not broad compatibility certification. Runtime diversity across API 26-35 and other OEMs, long-duration battery/thermal characterization, physical timeout injection, full accessibility tooling, large text, RTL, rotation/multi-window, traffic capture, and reproducible-build comparison remain follow-up work.

Disabling Android scan throttling removes Android's standard quota for local testing; it does **not** guarantee that the Wi-Fi chipset or OEM completes a fresh scan every five seconds. On API 26-28, and whenever Android cannot report the setting, five-second mode stays disabled. Faster requests may increase battery use.

## Candidate integrity

- APK: `Open-Scanner-v0.2.0.apk`
- Size: 2,025,741 bytes
- SHA-256: `15e690c0314feee3a5a2d925f676aed2e9887336f57d8f590ca9868179dd542c`
- Package: `org.openscanner.app`
- Version: 2 / 0.2.0; minimum API 26; target API 36
- Signing certificate SHA-256: `3d170c6970999581227c7f0f4423564310ea2afd9b8d38d72d046a4c314b489f`
- APK signatures: v2 and v3 verified; zip alignment verified

Full evidence and residual scope are recorded in the [v0.2.0 test evidence](v0.2.0-test-evidence.md) and [release checklist](v0.2.0-checklist.md).
